/**
 * authRoutes.js — Authentication Routes
 * POST /api/auth/signup
 * POST /api/auth/login
 * (Filled in Stage 1 — Auth)
 */
import express from "express";
const router = express.Router();
export default router;
