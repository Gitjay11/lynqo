/**
 * userRoutes.js — User Profile Routes
 * GET  /api/users/:id
 * PUT  /api/users/profile
 * (Filled in Stage 2 — User Profiles)
 */
import express from "express";
const router = express.Router();
export default router;
